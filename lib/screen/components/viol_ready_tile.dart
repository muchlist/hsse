import 'package:flutter/material.dart';

class ViolReadyTile extends StatelessWidget {
  const ViolReadyTile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
